syntax = "proto3";
package lavanet.lava.pairing;

option go_package = "github.com/lavanet/lava/x/pairing/types";

message StakeRangeToCUList {
    repeated StakeRangeToCU List = 1;
  }

message StakeRangeToCU {
    uint64 LowerStake = 1;
    uint64 UpperStake = 2;
    uint64 ComputeUnits = 3;
  }
