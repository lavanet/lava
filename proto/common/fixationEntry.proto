syntax = "proto3";
package lavanet.lava.common;

option go_package = "github.com/lavanet/lava/common/types";
import "gogoproto/gogo.proto"; 

message Entry {
    uint64 block = 1; // block the entry was created (functions as key)
    bytes marshaled_data = 2; // marshaled data of the entry
}