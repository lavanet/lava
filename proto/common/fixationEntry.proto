syntax = "proto3";
package lavanet.lava.common;

option go_package = "github.com/lavanet/lava/common/types";
import "gogoproto/gogo.proto"; 

message Entry {
    string index = 1;
    uint64 block = 2; // block the entry was created (functions as key)
    bytes marshaled_data = 3; // marshaled data of the entry
}

message UniqueIndex {
    uint64 id = 1;
    string package_unique_index = 2;
  }
  