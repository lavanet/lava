package chaintracker

const (
	// ChainTrackerDefaultMemory is the default number of blocks the chain tracker keeps in memory.
	// Relocated from protocol/rpcprovider for use without the full rpcprovider dependency.
	ChainTrackerDefaultMemory = 100
)
